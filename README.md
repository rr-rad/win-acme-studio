# 🛡️ WIN ACME Wildcard SSL Studio

یک پنل تحت وب فوق‌العاده حرفه‌ای، با طراحی دارک و انیمیشنی برای مدیریت و صدور گواهینامه‌های **Wildcard SSL (`*.domain.com`)** رایگان از مرجع رسمی **Let's Encrypt** بر روی سیستم‌عامل ویندوز، با استفاده از اسکریپت محبوب `acme.sh` و محیط محلی XAMPP.

این پروژه مشکل بزرگ اجرای اسکریپت‌های لینوکسی شل (`.sh`) را در محیط ویندوز از طریق سوییچ هوشمند به `Git Bash` حل می‌کند و پروسه دو مرحله‌ای احراز هویت دستی دی‌ان‌اس (DNS-01 Challenge) را به راحت‌ترین شکل ممکن به یک رابط کاربری وب تبدیل می‌سازد.

---

## ✨ ویژگی‌های کلیدی (Features)

*   **🎨 تم مدرن سایبرپانک:** طراحی واکنش‌گرا و انیمیشنی با استفاده از Tailwind CSS و FontAwesome.
*   **🌐 پشتیبانی کامل از Wildcard:** درخواست همزمان گواهی برای دامنه اصلی (`domain.com`) و تمام زیردامنه‌ها (`*.domain.com`).
*   **🔒 اجبار به استفاده از Let's Encrypt:** دور زدن ZeroSSL (که مرجع پیش‌فرض جدید acme.sh است) و هماهنگی کامل با سرورهای لتس‌انکریپت.
*   **⚙️ حل چالش سرویس ویندوز (XAMPP Service Fix):** اسکن و شناسایی هوشمند مسیر فایل‌ها در صورتی که آپاچی به صورت یک سرویس سیستمی (SYSTEM user) اجرا شود و فایل‌ها را در پوشه `System32` ذخیره کند.
*   **💻 کنسول لاگ خام:** امکان مشاهده خروجی مستقیم و زنده ترمینال در پایین صفحه برای عیب‌یابی سریع.
*   **📋 کپی با یک کلیک:** باکس‌های تفکیک‌شده و آماده برای کپی کردن سریع Private Key، Certificate و Full Chain.

---

## 📋 پیش‌نیازها (Prerequisites)

برای اجرای بدون نقص این پنل، باید مراحل زیر را روی ویندوز خود آماده داشته باشید:

### ۱. نصب Git برای ویندوز
از آنجا که `acme.sh` یک اسکریپت شل (`.sh`) لینوکسی است، برای اجرا در ویندوز به پایانه `Git Bash` نیاز دارد.
*   آخرین نسخه Git را از [وب‌سایت رسمی گیت](https://git-scm.com/download/win) دانلود و نصب کنید.
*   مسیر نصب پیش‌فرض باید `C:\Program Files\Git\bin\bash.exe` باشد. (اگر مسیر گیت شما متفاوت است، خط ۶ فایل `ssl.php` را تغییر دهید).

### ۲. نصب acme.sh از طریق Git Bash
۱. پایانه **Git Bash** را در ویندوز باز کنید.
۲. دستور زیر را برای کلون و نصب `acme.sh` در پوشه کاربری خود اجرا کنید (ایمیل خود را جایگزین کنید):
```bash
   git clone [https://github.com/acmesh-official/acme.sh.git](https://github.com/acmesh-official/acme.sh.git)
   cd ./acme.sh
   ./acme.sh --install -m myemail@example.com
```
### ۳. پس از نصب، اسکریپت شما باید در مسیر زیر قرار گرفته باشد:
```
   C:\Users\YOUR_USERNAME\.acme.sh\
```
### ۳. فعال بودن وب‌سرور XAMPP
*  مطمئن شوید XAMPP نصب است و سرویس Apache روشن است.

## 🚀 نحوه راه‌اندازی و استفاده (Installation & Usage)
## ۱. فایل ssl.php را دانلود کرده و در پوشه پروژه خود در هاد داکس زمپ کپی کنید:

```Plaintext
   C:\xampp\htdocs\test\ssl.php
```
### ۲. فایل ssl.php را با یک ادیتور باز کنید و در خط ۲، نام کاربری ویندوز خود را در متغیر $acme_dir اصلاح کنید:

```PHP
   $acme_dir = 'C:/Users/YOUR_WINDOWS_USERNAME/.acme.sh';
```
### ۳. مرورگر خود را باز کرده و به آدرس زیر بروید:

```Plaintext
   http://localhost/test/ssl.php
```
## 🛠️ مراحل صدور گواهی در پنل:
*  **مرحله اول (Issue):** نام دامنه خود را (مثلاً example.com) وارد کرده و دکمه بنفش‌رنگ را بزنید. پنل دو رکورد TXT مخصوص به شما تحویل می‌دهد.

*  **مرحله دوم (DNS Setup):** رکوردهای TXT دریافتی را در مدیریت DNS دامنه خود (مانند Cloudflare) با نام _acme-challenge به صورت دو رکورد جداگانه ست کنید.

* **مرحله سوم (Verify & Renew):** چند دقیقه صبر کنید تا DNSها در اینترنت منتشر شوند، سپس دکمه سبز رنگ Renew را در پنل کلیک کنید. گواهی‌ها ساخته شده و در باکس‌های پایینی آماده کپی خواهند بود!

## ⚠️ نکات بسیار مهم فنی
تغییرات زنجیره اعتماد (CA Chain): فایل ca.cer صادر شده جدید ممکن است حاوی ۳ بلوک متوالی باشد. این به دلیل ساختار جدید امنیت زنجیره‌ای لتس‌انکریپت است. شما باید هر ۳ بلوک را کاملاً کپی کرده و به عنوان CA Bundle / Chain در هاست خود قرار دهید یا به سادگی تمام محتویات باکس Full Chain را کپی و استفاده کنید.

تغییر مسیر خودکار در حالت سرویس: اگر XAMPP شما به صورت یک Service ویندوز اجرا شود، acme.sh به دلیل تغییر محیط کاربری، گواهی‌ها را به صورت خودکار در مسیر سیستم‌عامل یعنی C:\Windows\System32\config\systemprofile\.acme.sh\ ایجاد می‌کند. این اسکریپت مجهز به سیستم هوشمند دبل‌چک است و فایل‌ها را از هر دو مسیر به صورت خودکار اسکن کرده و بدون هیچ تداخلی لود می‌کند.

## 🤝 مشارکت (Contributing)
اگر ایده‌ای برای بهتر کردن ظاهر پنل، اضافه کردن APIهای خودکار کلودفلر (برای ست شدن اتوماتیک DNS بدون نیاز به روش دستی) یا هر قابلیت دیگری دارید، خوشحال می‌شوم پروژه را Fork کنید و Pull Request بفرستید!
